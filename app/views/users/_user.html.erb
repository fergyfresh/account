<li>
  <%= link_to user.email, user %>
  <%= render partial: 'roster_form', locals: { user: user } %>

  <% if current_user.admin? %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
